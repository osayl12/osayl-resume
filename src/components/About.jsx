export default function About() {
  return (
    <section id="about" className="card">
      <h2>About</h2>
      <p>
        Software Engineering student. I build web apps with React + Node.js, work with SQL databases,
        and deploy using Docker and GitHub Actions. I deploy to Oracle Cloud Ubuntu (ARM64) via SSH
        and automate maintenance with Cron jobs.
      </p>
    </section>
  );
}
